<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navigationwithtoolbar">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".SendQuestionActivity"></activity>
        <activity android:name=".ExoPlayerActivity" />
        <activity android:name=".pimDataActivity" />
        <activity android:name=".pimActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".BuySellStock" />
        <activity android:name=".VerifyEmail" />
        <activity android:name=".SignUp" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>